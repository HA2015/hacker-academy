<% provide(:title, 'Members') %>

<% if logged_in? && current_user.site_admin?%>
    <p>
      Download emails:
      <%= link_to "CSV", users_path(format: "csv") %> |
      <%= link_to "Excel", users_path(format: "xls") %>
    </p>
<% end %>

<!-- list execs -->
	<div class="row">
		<div class="col-md-4 col-sm-4">
			<h2>
				Executive Team
			</h2>
			<%= render @exec_users %>
		</div>
		<div class="col-md-4 col-sm-4">	
			<!-- list sponsors -->
			<h2>
				Our Sponsors
			</h2>
			<%= render @sponsor_users %>
		</div>
		<div class="col-md-4 col-sm-4">	
			<!-- list members -->
			<h2>
				Club Members
			</h2>
  			<%= render @users %>
		</div>	
	</div>

<!-- Space buffer for the footer -->
<% 25.times do %>
<br>
<% end %>